# 換班功能：程式碼與資料庫邏輯詳細說明

本系統的「換班申請」功能採用了**非同步申請-審核機制**，確保每一筆班次異動都有完整的紀錄，並在審核通過後自動更新正式班表。以下是從資料庫到前端的完整解析。

## 1. 資料庫設計 (Database Schema)

換班功能主要涉及兩張資料表：`schedules`（正式班表）與 `swap_requests`（換班申請紀錄）。

### A. 換班申請表 (`swap_requests`)
這張表負責儲存所有「處理中」與「已完成」的申請。
- `id`: 申請單流水號。
- `requester_id`: 發起人 ID。
- `target_user_id`: 換班對象 ID。
- `schedule_id`: 想要交換的原始班次 ID（關聯到 `schedules`）。
- `status`: 當前狀態，預設為 `pending`（待處理），可變更為 `accepted`（已接受）或 `rejected`（已拒絕）。
- `reason`: 換班理由（選填）。

### B. 正式班表 (`schedules`)
當換班成功後，這張表的 `user_id` 會被修改。
- `id`: 班次流水號。
- `user_id`: 負責該班次的員工 ID。
- `shift_id`: 班次定義（時間、角色等）。

---

## 2. 後端邏輯 (Backend Logic)

實作於 `backend/index.js`，包含三個核心 API。

### A. 發起換班 (`POST /api/swap`)
當員工在前端點擊「發送申請」時：
1. 接收 `requesterId`, `targetUserId`, `scheduleId` 等參數。
2. 在 `swap_requests` 插入一條 `status = 'pending'` 的紀錄。

### B. 取得申請列表 (`GET /api/swap-requests/:userId`)
為了讓使用者看到「誰想跟我換」以及「我發給誰」，這個 API 使用了 `JOIN` 來抓取姓名與班次時間：
```sql
SELECT sr.*, u.name as requester_name, sh.start_time, sh.end_time, sh.day_of_week
FROM swap_requests sr
JOIN users u ON sr.requester_id = u.id
JOIN schedules s ON sr.schedule_id = s.id
JOIN shifts sh ON s.shift_id = sh.id
WHERE sr.target_user_id = ? OR sr.requester_id = ?
```

### C. 審核申請並自動更新班表 (`PUT /api/swap/:id`)
這是最關鍵的邏輯。當對象點擊「接受」時：
1. **更新申請狀態**：將 `swap_requests` 的 `status` 改為 `accepted`。
2. **自動轉讓班次**：系統會執行一段 SQL 更新 `schedules` 表：
   ```sql
   UPDATE schedules SET user_id = [新員工ID] WHERE id = [原始班次ID]
   ```
3. **即時生效**：此動作完成後，首頁的「當週班表」會立刻看到該時段的負責人已更換。

---

## 3. 前端流程 (Frontend Workflow)

實作於 `frontend/src/pages/Swap.jsx`。

### 步驟一：選擇班次與對象
- 前端會先調用 `GET /api/schedules` 找出目前登入者「自己的班次」。
- 同時調用 `GET /api/users` 呈現同事名單（排除自己）。

### 步驟二：狀態監控
- 頁面區分為「待處理申請」與「我發出的申請」。
- 透過 `useEffect` 定時或手動觸發 `fetchRequests` 重新載入最新狀態。

### 步驟三：即時回饋
- 當使用者點擊「接受」按鈕，前端發送 `PUT` 請求。
- 成功後，前端會顯示成功訊息，並引導使用者回首頁查看已更新的 Timeline 班表。

---

## 4. 邏輯特點總結

1. **原子性**：班次的更換是在換班申請被「接受」的瞬間同步完成的，不會出現「申請過了但班表沒改」的情況。
2. **透明化**：發起人與對象都能在介面上看到申請的進度。
3. **資料一致性**：透過 SQL 的 `JOIN` 關聯，確保顯示的班次時間與負責人永遠是資料庫中最新、最正確的版本。
